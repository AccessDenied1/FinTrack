package com.sethv.fintrack.core.ui.util

/**
 * Kept for source compatibility — the canonical implementation now lives in
 * core:common so non-UI modules (notifications) share the exact same format.
 */
typealias Format = com.sethv.fintrack.core.common.util.Format
