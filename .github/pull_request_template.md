## What changed
<!-- feat/fix + files:line refs -->

## Why

## Verification
- [ ] `./gradlew testDebugUnitTest` green
- [ ] `./gradlew assembleDebug` green
- [ ] Manual test (SMS sample / screen)

## Screenshots (if UI)

## Checklist
- [ ] Targets `dev` (not `main`)
- [ ] `versionCode`/`versionName` untouched (maintainer bumps on release)
- [ ] No `smsBody` logged at INFO
